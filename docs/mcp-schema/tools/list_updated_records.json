{
  "description": "Read the NetHunt updated-record feed for a folder, optionally filtered by field names.",
  "inputSchema": {
    "properties": {
      "field_names": {
        "anyOf": [
          {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional list of exact field names to filter update or change feeds by.",
        "title": "Field Names"
      },
      "folder_id": {
        "description": "NetHunt folder identifier. Discover valid values with `list_readable_folders` or `list_writable_folders`.",
        "title": "Folder Id",
        "type": "string"
      },
      "limit": {
        "anyOf": [
          {
            "minimum": 1,
            "type": "integer"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional maximum number of items to return when the endpoint supports limiting.",
        "title": "Limit"
      },
      "since": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional feed cursor or timestamp accepted by the underlying NetHunt trigger endpoint.",
        "title": "Since"
      }
    },
    "required": [
      "folder_id"
    ],
    "title": "list_updated_recordsArguments",
    "type": "object"
  },
  "name": "list_updated_records",
  "outputSchema": {
    "additionalProperties": true,
    "title": "list_updated_recordsDictOutput",
    "type": "object"
  }
}
