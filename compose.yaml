services:
  nethunt-mcp-http:
    build:
      context: .
    image: nethunt-mcp:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MCP_TRANSPORT: streamable-http
      MCP_HOST: 0.0.0.0
      MCP_PORT: 8000
    ports:
      - "8000:8000"
